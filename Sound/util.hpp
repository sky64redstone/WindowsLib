#ifndef WINDOWS_SOUND_UTIL_HPP
	#define WINDOWS_SOUND_UTIL_HPP

	#include <windows.h>
	#include <mmsystem.h>

	#pragma comment(lib, "winmm.lib")

	#include <memory>
	#include <string>
	#include <fstream>
	#include <vector>
	#include <list>

	#undef max
	#undef min
#endif